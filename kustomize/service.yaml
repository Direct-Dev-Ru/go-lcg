apiVersion: v1
kind: Service
metadata:
  name: lcg
  namespace: lcg
  labels:
    app: lcg
    version: v2.0.4
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: lcg
    version: v2.0.4
